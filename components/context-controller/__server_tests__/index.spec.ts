import ContextController from '..'

describe('<ContextController />', () => {
  it('fancy test', () => {
    expect(true).toBe(true)
  })

  it('should be defined', () => {
    expect(typeof ContextController).toBe('function')
  })
})
